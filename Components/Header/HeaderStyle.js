import {StyleSheet } from 'react-native'

const HeaderStyle = StyleSheet.create({
    wrapper: {
        backgroundColor: '#000',
    },
    content: {
        textAlign: 'center',
        fontWeight: '500',
        fontSize: 18,
        padding: 10,
        color: 'white'
    }
});

export default HeaderStyle