import {StyleSheet } from 'react-native'

const HeaderStyle = StyleSheet.create({
    wrapper: {
        backgroundColor: '#000',
    },
    content: {
        textAlign: 'center',
        fontWeight: '500',
        fontSize: 18,
        padding: 20,
        color: 'white'
    }

})

export default HeaderStyle