import {StyleSheet } from 'react-native'

const SinglePhotoStyle = StyleSheet.create({
    wrapper: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
});

export default SinglePhotoStyle